# Runtime do Coletor (GitHub Actions). Testes usam só pandas+pytest (ver dev-requirements).
# 0.41.4 é a versão validada contra demos reais (e a mais nova no PyPI em 2026-07).
demoparser2>=0.41.4,<1
boto3>=1.43.53
psycopg[binary]>=3.2
# Auditoria finding #6: versão travada (era solta, sem teto nem piso) — 4.3 é a
# validada em dev (ver `pip show rarfile`); trava evita uma versão futura mudar
# comportamento de extração (ex.: tool_setup) sem aviso.
rarfile==4.3
