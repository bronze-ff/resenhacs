interface:
  display_name: "Resenhacs"
  short_description: "Repo-specific patterns and workflows for resenhacs"
  default_prompt: "Use the resenhacs repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true